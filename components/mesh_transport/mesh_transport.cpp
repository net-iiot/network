#include "mesh_transport.hpp"
#include "esp_log.h"

namespace monimesh {
static const char* TAG = "MeshTransport";

esp_err_t MeshTransport::init() {
    ESP_LOGI(TAG, "MeshTransport init (placeholder)");
    return ESP_OK;
}
}
